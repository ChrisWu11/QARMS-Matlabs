# Model File

`fruit_yolo_best.pt` is the trained YOLO detector used by the MATLAB/Simulink
bridge in `python/yolo_simulink.py`.

The detector maps classes to MATLAB numeric codes as follows:

- `0`: no fruit detected
- `1`: strawberry
- `2`: banana
- `3`: tomato
